# Remote server configuration
REMOTE_HOST=your_remote_server_ip
REMOTE_PORT=22
REMOTE_USERNAME=your_ssh_username
REMOTE_PASSWORD=your_ssh_password
# Optional: Path to SSH key file (if using SSH key authentication)
SSH_KEY_PATH=path/to/your/private_key 